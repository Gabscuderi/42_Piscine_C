#include <

void	ft_putchar(char c)

	void 	print_line( int x, int sx, int  mdl, int dx)
{
	int siigentile;
		siigentile  =  1;
		if (x > 0)
		{
			ft_putchar (sx);
		}
		while (siigentile < (x-1))
		{
			ft_putchar(mdl);
			siigentile++;
		}
		if (x > 1)
		{
			ft_putchar(dx)
		}
		ft putchar('\n')

void	rush(int x, int y)
{
	int tiprego
		tiprego = 1
		if(x > 0 && y > 0)
		{
			print_line(x, 'A', 'B', 'A');
		}
		while (space <(y -1) && x > 0)
		{
			print_line(x, 'B', ' ', 'B');
			tiprego++;
		}
		if (y > 1 && x > 0)
		{
		print_line(x, 'C', 'B', 'C');
		}
}
